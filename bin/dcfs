#!/usr/bin/env ruby

require 'rubygems'
require 'bundler'
Bundler.setup :default

require 'dcfs'

root = DCFS::Root.new
FuseFS.set_root root

# Mount under a directory given on the command line.
FuseFS.mount_under File.expand_path(ARGV.shift)
root.spawn_client_process

FuseFS.run
